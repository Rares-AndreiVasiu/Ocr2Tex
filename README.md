# Ocr2Tex



## Ocr2Tex is project which allows you to extract text from images, then convert it into structured and customizable latex code.



------

## Requirements

In Google Collab, install the following dependencies: 

```bash
!pip install -q git+https://github.com/huggingface/transformers.git
!pip install -q torch torchvision Pillow tqdm accelerate
```

